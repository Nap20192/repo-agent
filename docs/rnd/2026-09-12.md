## ECC R&D — 2026-09-12 — ECC 2.2.1
catalog: 291 skills, 68 agents, 94 commands (`/home/vnkjd/.claude/plugins/cache/ecc/ecc/2.2.1`)

### Novelties
_baseline (first snapshot) or no changes since the last snapshot_

### Candidates for repo-agent2 (keyword score ≥ 2; `used` = already referenced here)
- [5] skill **orch-pipeline** — Shared orchestration engine for the orch-* skill family. Defines the gated Research-Plan-TDD-Review-Commit pipeline, the size classifier, the agent ma
- [4] agent **harness-optimizer** — Improve local agent-harness configuration reliability and cost using eval-driven grading (pass@k/pass^k) derived from the eval-harness skill.
- [4] command **cpp-review** — Comprehensive C++ code review for memory safety, modern C++ idioms, concurrency, and security. Invokes the cpp-reviewer agent.
- [4] command **orch-build-mvp** — Orchestrate bootstrapping a working MVP from a design/spec doc — ingest, slice, scaffold, TDD, review, gated commit (reuses the GAN harness). Wrapper 
- [4] command **python-review** — Comprehensive Python code review for PEP 8 compliance, type hints, security, and Pythonic idioms. Invokes the python-reviewer agent.
- [4] command **react-review** — Comprehensive React/JSX code review for hook correctness, render performance, server/client component boundaries, accessibility, and React-specific se
- [4] skill **docker-patterns** — Docker and Docker Compose patterns for local development, hardened CLI installer harnesses, container security, networking, volumes, and multi-service
- [4] skill **orch-add-feature** (used) — Orchestrate building a brand-new feature end to end — research, plan, TDD implementation, review, and gated commit — by delegating each phase to the m
- [4] skill **security-scan** — Scan your Claude Code configuration (.claude/ directory) for security vulnerabilities, misconfigurations, and injection risks using AgentShield. Check
- [3] agent **gan-evaluator** — GAN Harness — Evaluator agent. Tests the live running application via Playwright, scores against rubric, and provides actionable feedback to the Gener
- [3] agent **gan-generator** — GAN Harness — Generator agent. Implements features according to the spec, reads evaluator feedback, and iterates until quality threshold is met.
- [3] agent **gan-planner** — GAN Harness — Planner agent. Expands a one-line prompt into a full product specification with features, sprints, evaluation criteria, and design direc
- [3] agent **mle-reviewer** — Production machine-learning engineering reviewer for data contracts, feature pipelines, training reproducibility, offline/online evaluation, model ser
- [3] agent **python-reviewer** — Expert Python code reviewer specializing in PEP 8 compliance, Pythonic idioms, type hints, security, and performance. Use for all Python code changes.
- [3] agent **rag-pipeline-reviewer** — Reviews RAG (Retrieval-Augmented Generation) pipelines for retrieval quality, chunking strategy, embedding choices, and evaluation coverage. Invoke wh
- [3] agent **react-reviewer** — Expert React/JSX code reviewer specializing in hook correctness, render performance, server/client component boundaries, accessibility, and React-spec
- [3] agent **security-reviewer** — Security vulnerability detection and remediation specialist. Use PROACTIVELY after writing code that handles user input, authentication, API endpoints
- [3] command **flutter-review** — Review Flutter/Dart code for idiomatic patterns, widget best practices, state management, performance, accessibility, and security. Invokes the flutte
- [3] command **go-review** — Comprehensive Go code review for idiomatic patterns, concurrency safety, error handling, and security. Invokes the go-reviewer agent.
- [3] command **kotlin-review** — Comprehensive Kotlin code review for idiomatic patterns, null safety, coroutine safety, and security. Invokes the kotlin-reviewer agent.
- [3] command **orch-add-feature** (used) — Orchestrate building a brand-new feature end to end — research, plan, TDD, review, gated commit. Wrapper that kicks off the orch-add-feature skill.
- [3] command **security-scan** — Run AgentShield against agent, hook, MCP, permission, and secret surfaces.
- [3] command **vue-review** — Comprehensive Vue.js code review for Composition API correctness, reactivity, composable patterns, template security, accessibility, and Vue-specific 
- [3] skill **agent-architecture-audit** — Full-stack diagnostic for agent and LLM applications. Audits the 12-layer agent stack for wrapper regression, memory pollution, tool discipline failur
- [3] skill **agent-eval** — Head-to-head comparison of coding agents (Claude Code, Aider, Codex, etc.) on custom tasks with pass rate, cost, time, and consistency metrics. Use wh
- [3] skill **agent-sort** — Build an evidence-backed ECC install plan for a specific repo by sorting skills, commands, rules, hooks, and extras into DAILY vs LIBRARY buckets usin
- [3] skill **agentic-engineering** — Operate as an agentic engineer using eval-first execution, decomposition, and cost-aware model routing. Use when planning or executing engineering wor
- [3] skill **autonomous-agent-harness** — Transform Claude Code into a fully autonomous agent system with persistent memory, scheduled operations, computer use, and task queuing. Replaces stan
- [3] skill **autonomous-loops** — Patterns and architectures for autonomous Claude Code loops — from simple sequential pipelines to RFC-driven multi-agent DAG systems. Retained for com
- [3] skill **continuous-agent-loop** — Patterns for continuous autonomous agent loops with quality gates, evals, and recovery controls. Use when running an agent loop that must self-check, 
- [3] skill **dmux-workflows** — Multi-agent orchestration using dmux (tmux pane manager for AI agents). Patterns for parallel agent workflows across Claude Code, Codex, OpenCode, and
- [3] skill **dynamic-workflow-mode** — Design task-local harnesses, eval gates, and reusable skill extraction for Claude dynamic workflow mode and other adaptive agent harnesses. Use when b
- [3] skill **gan-style-harness** — GAN-inspired Generator-Evaluator agent harness for building high-quality applications autonomously. Based on Anthropic's March 2026 harness design pap
- [3] skill **loop-design-check** — Design a goal-oriented agent loop, and review it for the ways loops go wrong — spinning and burning tokens, Goodhart-gaming the verifier, or running a
- [3] skill **orch-build-mvp** — Orchestrate bootstrapping a working MVP from a design or spec document — ingest the doc, plan thin vertical slices, scaffold the first end-to-end slic
- [3] skill **orch-fix-defect** — Orchestrate fixing a bug — reproduce it as a failing regression test, fix to green, review, and gated commit — by delegating each phase to the matchin
- [3] skill **pytorch-patterns** — PyTorch deep learning patterns and best practices for building robust, efficient, and reproducible training pipelines, model architectures, and data l
- [3] skill **quarkus-verification** — Verification loop for Quarkus projects: build, static analysis, tests with coverage, security scans, native compilation, and diff review before releas
- [3] skill **ralphinho-rfc-pipeline** — RFC-driven multi-agent DAG execution pattern with quality gates, merge queues, and work unit orchestration. Use when running RFC-driven multi-agent ex
- [3] skill **santa-method** — Multi-agent adversarial verification with convergence loop. Two independent review agents must both pass before output ships. Use when output must cle

## R&D team pass — 2026-09-12 (skill-scout + skill-stocktake + harness-audit)

### Update path
Installed: ECC 2.2.1 (scope local), `claude plugin marketplace update` → 10 marketplaces refreshed, `claude plugin update ecc@ecc --scope local` → already latest. CHANGELOG "Unreleased" carries only a Windows device-id fix. Next run of `scripts/ecc_rnd.py` after an upgrade will list the real novelties (baseline saved today).

### Stocktake — what repo-agent2 already uses (BOARD.md, memory `feedback-use-ecc-skills`)
- skills: team-agent-orchestration, orch-add-feature (orch-pipeline engine), tdd-workflow, dev-team, git-explorer (user's own)
- agents: planner, code-reviewer, python-reviewer, security-reviewer, silent-failure-hunter
- hooks (plugin-wide): GateGuard fact-forcing on Bash/Edit/Write, cost warnings on Agent spawns
- unused but installed: 286 skills, 63 agents, ~90 commands — see candidates above; the ones that matter are below.

### Harness audit (deterministic, `scripts/harness-audit.js repo`, rubric 2026-05-19): **8/39**
Tool Coverage 6/10 · Context Efficiency 4/10 · Quality Gates 0/10 · Memory Persistence 0/10 · Eval Coverage 0/10 · Security Guardrails 3/10 · GitHub Integration 0/10 (16 checks, 13 failing).
The rubric looks for its own markers (`tests/` is there but no `test` script in pyproject, no CLAUDE.md/AGENTS.md, no `.claude` hooks/commands, no CI, no eval config, no SECURITY.md, no git repo at all), so the score understates the project (63 pytest tests, gates in code). Still, its Top 3 are cheap and real.

### Proposals (verdict · effect · effort)
| # | ECC surface | Verdict | Expected effect | Effort |
|---|---|---|---|---|
| 1 | **CLAUDE.md / AGENTS.md** for this repo (harness-audit Top 3; `ecc:configure-ecc`, `ecc:agent-sort` to pick the install plan) | apply | every session (and every subagent) starts with the pipeline, gates, env knobs, `uv run pytest`; stops re-deriving the contract from BOARD.md | S |
| 2 | **`git init` + `ecc:git-workflow` + `/pr`** | apply | GATE 2 of orch-pipeline becomes real; GitHub Integration 0→n/a; enables `/code-review` on diffs instead of whole-tree reviews | S |
| 3 | **`ecc:verification-loop`** as the merge gate of every card (pytest + ruff + `scripts/ecc_rnd.py --selftest` + gopls smoke) | apply | one command answers "is it done" instead of ad-hoc test runs; feeds Quality Gates | S |
| 4 | **`ecc:hookify-rules`** — project hooks: block edits to `.env`, forbid `docker stop` / `pkill -f` patterns that bit us, warn when `scanner/core` imports `adapter`/`app` (layer rule) | apply | the archtest we lost from the Go original, enforced at edit time; Security Guardrails 3→6 | M |
| 5 | **`ecc:eval-harness` + `harness-optimizer` agent** (pass@k on eval/dataset.json per pipeline node; card 19 already builds the live harness) | apply | eval-driven changes to prompts/budgets; Eval Coverage 0→10; blocked only by the LLM key quota | M |
| 6 | **`ecc:cost-tracking` / `/cost-report`** (stop:cost-tracker hook) | apply | per-run token/cost numbers for the SLA question dev-team raised (PM/QA) | S |
| 7 | **`ecc:continuous-learning-v2`** + `/instinct-status` + `/evolve` | defer | would turn recurring lessons (zsh word-splitting, pkill self-match, secret redaction) into instincts; needs the observer hook enabled — do after #4 so hooks are reviewed together | M |
| 8 | **`ecc:security-scan`** (AgentShield over `.claude/`, hooks, MCP, secrets) | apply | catches `.env` key exposure and hook misconfig before sharing the repo; run once now, then per release | S |
| 9 | **`ecc:agent-harness-construction`** (tool definitions / observation formatting review) | apply | our 15+ tools per agent: docstrings, caps, error shapes — a structured pass would trim tokens further (complements card 15 tool window) | M |
| 10 | **`ecc:santa-method`** (two independent reviewers must both pass) for Critic design | defer | would formalize Critic ⇄ Investigator disagreement; only after eval shows Critic's precision | M |
| 11 | **`ecc:loop-design-check`** on `PipelineV2` queue loop + `/loop` for ecc-rnd | apply | review of stop conditions / budget burn in the Investigator loop; cheap read-only pass | S |
| 12 | **`ecc:agent-architecture-audit`** (12-layer agent stack diagnostic) | defer | broad; run after #1–#4 so the audit reads a configured harness | L |
| 13 | **`ecc:docker-patterns`** for the sandbox (dev-team blocker: `shell` on host) | apply | the container-per-run sandbox from the ideal-system plan; pair with `ecc:kubernetes-patterns` only if CI needs it | L |
| 14 | gan-* agents, `dmux-workflows`, `ralphinho-rfc-pipeline`, `autonomous-agent-harness` | reject | UI/GAN loops and RFC-DAG orchestration do not fit a CLI scanner; team-agent-orchestration + orch-* already cover coordination | — |
| 15 | language review commands (cpp/react/vue/kotlin/flutter/go-review) | reject | not our stack (Go is a scan *target*, not our code) | — |

### Top 5 to do next
1. CLAUDE.md + git init (#1, #2) — unblocks GATE 2 and gives every agent the contract.
2. verification-loop as the standard merge gate (#3).
3. hookify rules for .env / destructive shell / layer rule (#4).
4. cost-tracking hook + /cost-report (#6).
5. security-scan of `.claude` and the repo before publishing (#8).

Schedule: `/loop 1d /ecc-rnd` in a session, or the `schedule` skill for a cloud routine; the script alone is safe to run any time (`uv run python scripts/ecc_rnd.py`).
