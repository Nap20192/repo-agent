"""Secondary adapters (static scanners, SQLite State, OWASP tables) and the primary one (agent tools).

Addressed by submodule on purpose: `from scanner.adapter import scanners` / `.store` / `.tools` / `.owasp`."""
