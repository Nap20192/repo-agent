"""Tool roster of `domain`: the run's domain map, then the code around the entity."""

ROSTER = (
    "domain_map",
    "read_file",
    "grep",
    "lsp_symbols",
    "lsp_definition",
    "lsp_references",
)
